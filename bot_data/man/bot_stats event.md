Get event counts for either all events or a specified event.

Arguments:
* `event`: The event to get stats for. Leave blank for all events.

Examples:
* `{prefix}bot_stats event`
* `{prefix}bot_stats event message`
