Remove the color of every role that isn't a color role.

Recommended to put the bots role at the *very* top before using for maximum results (can be moved back down later).

Example: `{prefix}color clean`
